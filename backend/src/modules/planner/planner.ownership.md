# Planner backend ownership

Frontend page:

- `src/pages/PlannerPage.tsx`

Backend scope:

- `TrainingSession`
- planner CRUD endpoints
- calendar filtering
- planner-specific validation rules

Current routes:

- `GET /api/planner/sessions`
- `POST /api/planner/sessions`
- `PUT /api/planner/sessions/:sessionId`
- `DELETE /api/planner/sessions/:sessionId`

